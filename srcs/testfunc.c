#include "../includes/filler.h"

// FILE	*f;

// void	print_queue(t_queue *a, int size_a, t_queue *b, int size_b)
// {
// 	f = fopen("test", "w");
// 	fprintf(f, "x:%d y:%d\n", a->first->coor.x, a->first->coor.y);
// 	fprintf(f, "x:%d y:%d\n", a->last->coor.x, a->last->coor.y);
// 	fprintf(f, "x:%d y:%d\n", b->first->coor.x, b->first->coor.y);
// 	fprintf(f, "x:%d y:%d\n", b->last->coor.x, b->last->coor.y);
// }

	// f = fopen("test", "w");
	// for (int i = 0; i < filler->height; i++)
	// {
	// 	for (int j = 0; j < filler->width; j++)
	// 		fprintf(f, "%d", filler->visited[i][j]);
	// 	fprintf(f, "\n");
	// }
	// fprintf(f, "x:%d y:%d\n", queue_a->first->coor.x, queue_a->first->coor.y);
	// fprintf(f, "x:%d y:%d\n", queue_a->first->next->coor.x, queue_a->first->next->coor.y);
	// fprintf(f, "x:%d y:%d\n", queue_a->first->next->next->coor.x, queue_a->first->next->next->coor.y);
	// fprintf(f, "x:%d y:%d\n", queue_a->first->next->next->next->coor.x, queue_a->first->next->next->next->coor.y);
